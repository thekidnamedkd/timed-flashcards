export class Deck {

  constructor(deckArray) {
    // this.deckArray = []; //will contain X number of populated flashcards 
  }

  flashcard () {
    this.definitionId = definitionId; //definition orients card never changes (id = def01)
    this.termId = termId; // correct term id to match definition id (id = term101)
    this.phonyTermGrab = [] //will contain two random phony terms from real term Id's of other cards ([id = term105, term 110])
  }

  
}